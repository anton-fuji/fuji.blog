---
interface Props {
  label: string;
  href?: string;
  kind?: 'meta' | 'tag';
}

const { label, href, kind = 'tag' } = Astro.props;

const classes = [
  'post-badge',
  'badge',
  'badge-sm',
  kind === 'tag'
    ? 'post-badge-tag badge-outline badge-primary'
    : 'post-badge-meta badge-ghost',
];
---

{href ? (
  <a href={href} class:list={classes}>
    {kind === 'tag' && <span class="badge-hash">#</span>}
    <span>{label}</span>
  </a>
) : (
  <span class:list={classes}>
    {kind === 'tag' && <span class="badge-hash">#</span>}
    <span>{label}</span>
  </span>
)}
