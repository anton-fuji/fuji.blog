---
import { getCollection, render } from 'astro:content';
import BlogPost from '../../layouts/BlogPost.astro';
import { getPostLocale, getPostSlug, getTranslatedPost } from '../../lib/posts';
import LinkCard from '../../components/LinkCard.astro';
import Quote from '../../components/Quote.astro';

export async function getStaticPaths() {
  const posts = await getCollection('posts');
  return posts
    .filter((post) => getPostLocale(post) === 'ja')
    .map((post) => ({
      params: { id: getPostSlug(post) },
      props: { post, posts },
    }));
}

const { post, posts } = Astro.props;
const { Content , headings} = await render(post);
const translatedPost = getTranslatedPost(post, posts, 'en');
const slug = getPostSlug(post);
---

<BlogPost {...post.data} slug={slug} headings={headings} post={post} lang="ja" jaPath={`/posts/${slug}`} enPath={translatedPost ? `/en/posts/${getPostSlug(translatedPost)}` : undefined}>
  <Content components={{ LinkCard, Quote }} />
</BlogPost>
